# Anchor: working-compile-2026-07-05

Commit / Tag: anchor/working-2026-07-05

Datum: 2026-07-05

Beschrijving:

- Dit ankerpunt bevat de actuele code die op dit moment compileert (Kotlin compile successful) en waarin de normale werking van de app grotendeels intact is.
- Bekende resterende issues: CSV-import bevat nog mappings/fk problemen (importfouten bij bepaalde velden). Deze worden in een volgende sessie opgelost.
- Er zijn geen runtime-starting-crashes bekend bij deze build op de testomgeving, maar import/foreign key mismatches moeten nog worden getest en gefixed.

Opnames / hoe te reproduceren:

1. Stand in repository: dit anker is gemaakt vanaf de huidige `VT5-Vico` branch HEAD (gelokaliseerde commit wordt getagd als `anchor/working-2026-07-05`).
2. Bouw de debug APK: 

```powershell
Set-Location -LiteralPath 'C:\AndroidApps\VoiceTally'
.\gradlew.bat :app:assembleDebug
```

3. Installeer en start op een apparaat/emulator:

```powershell
adb install -r app\build\outputs\apk\debug\app-debug.apk
```

4. Test: controleer dat de app start en dat hoofdfuncties (navigatie, telling starten) werken. Test CSV-import met sample-bestanden; import kan nog fouten geven — zie logs voor details.

Notes:
- Dit is een lokaal anker (tag) en is nog niet per se naar remote gepusht; als je wil dat ik deze tag ook naar remote (origin) push, geef dan aan welke remote en ik voer `git push origin anchor/working-2026-07-05` uit.
- Bestanden die nog aandacht nodig hebben voor importmapping: zie code in `CsvImportManager.kt` en Room entiteiten (waarnemingen/telling_headers) — deze zijn in backlog.

---

Generated by maintenance action on 2026-07-05.

